/// Connection Type, Used to Indentify What Kind of Connection do the User Uses
enum ConnectionType {
  Wifi,
  Mobile,
}

/// Dialog Status Type after Doing something
enum DialogStatusType {
  Success,
  Fail,
}
