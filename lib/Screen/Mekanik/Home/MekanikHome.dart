import 'package:flutter/material.dart';

class MekanikHome extends StatelessWidget {
  const MekanikHome({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Mekanik Home"),
      ),
    );
  }
}
