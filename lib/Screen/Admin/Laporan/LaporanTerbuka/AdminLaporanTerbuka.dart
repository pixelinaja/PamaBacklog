import 'package:flutter/material.dart';

class AdminLaporanTerbuka extends StatelessWidget {
  const AdminLaporanTerbuka({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("Admin Laporan Terbuka"),
        ),
      ),
    );
  }
}
