import 'package:flutter/material.dart';

class AdminLogLaporan extends StatelessWidget {
  const AdminLogLaporan({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("Admin Log Laporan"),
        ),
      ),
    );
  }
}
