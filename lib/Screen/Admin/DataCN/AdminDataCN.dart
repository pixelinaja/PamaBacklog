import 'package:flutter/material.dart';

class AdminDataCN extends StatelessWidget {
  const AdminDataCN({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("Admin Data CN"),
        ),
      ),
    );
  }
}
