import 'package:flutter/material.dart';

class GLLaporan extends StatelessWidget {
  const GLLaporan({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("Log Laporan"),
        ),
      ),
    );
  }
}
